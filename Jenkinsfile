pipeline {
  agent any
  stages {
    stage('init'){
      stages {
        script {
          echo 'Hello Jenkins'
          sh 'node --version'
          sh 'npm --version'
          echo "Hello Jenkins"
        }
      }
    }
  }

}
