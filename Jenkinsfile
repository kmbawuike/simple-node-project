pipeline {
  agent any
  stages {
    stage('init'){
      steps {
        script {
          echo 'Hello Jenkins'
          sh 'node --version'
          sh 'npm --version'
          echo "Hello Jenkins"
        }
      }
    }
  }

}
